# マップ素材

ステージセレクト画面の背景画像を置くフォルダです。

`map-assets.js` の `background` を変更すると、マップ画像を差し替えられます。

使える形式:

- PNG
- WebP
- SVG
- JPEG

ステージボタンの位置は `map-assets.js` の `nodes` で変更します。
`x` と `y` はマップ左上を基準にしたパーセント指定です。
